{
  "name": "backend",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && npm install && cd ../frontend && npx expo export --platform web",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.12",
    "yahoo-finance2": "3.10.0"
  },
  "devDependencies": {
    "wrangler": "4.44.0"
  }
}
